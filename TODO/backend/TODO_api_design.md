# TODO — Дизайн API (Virtual Screening)

## Ендпоінти запуску та статусу
- [ ] Ендпоінт `POST /screening-jobs` (запуск нового скринінгу з параметрами: бібліотека, reference actives, пороги) — **P0** | ~3h
- [ ] Ендпоінт `GET /screening-jobs/{id}` (статус і прогрес по стадіях funnel) — **P0** | ~2h
- [ ] Ендпоінт `GET /screening-jobs/{id}/results` (top-N результатів зі скорами) — **P0** | ~2h
- [ ] Ендпоінт `GET /screening-jobs` (список запусків користувача з фільтрацією/пагінацією) — **P1** | ~2h
- [ ] Ендпоінт `DELETE /screening-jobs/{id}` (скасування/видалення job) — **P2** | ~1h

## Схеми та валідація
- [ ] Pydantic-схеми запиту на створення job (валідація порогів, розміру бібліотеки) — **P0** | ~2h
- [ ] Pydantic-схеми відповіді для статусу і результатів — **P0** | ~2h
- [ ] Обробка помилок (некоректні параметри, неіснуючий job_id) з осмисленими HTTP-кодами — **P1** | ~2h

## Документація API
- [ ] Автогенерована OpenAPI-документація (FastAPI /docs) з прикладами запитів — **P2** | ~1h
- [ ] Усі повідомлення про помилки API та OpenAPI-документація англійською мовою (продукт англомовний, українська — лише в UI-локалі) — **P1** | ~1h

### Примітки
- API проєктується так, щоб фронтенд міг постійно опитувати (poll) статус job без додаткових ендпоінтів.

### Залежності
- Залежить від `backend/TODO_database.md` (схема таблиць) та `backend/TODO_job_queue_pipeline.md` (фактичне виконання job).
- `frontend/TODO_state_data_layer.md` (API-клієнт, polling) напряму залежить від ендпоінтів, визначених тут.
