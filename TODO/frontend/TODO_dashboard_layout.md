# TODO — Layout дашборду (Virtual Screening)

## Навігація та маршрутизація
- [ ] Розробити структуру навігації дашборду (запуск job / статус job / результати / історія запусків) — **P0** | ~2h
- [ ] Налаштувати client-side routing між сторінками job-launch / job-status / results / history — **P0** | ~2h

## Layout-регіони
- [ ] Спроєктувати layout-регіони дашборду (header, бокова навігація, основна область контенту, footer) — **P0** | ~2h
- [ ] Адаптивний layout для desktop (основний use case) з мінімальною підтримкою планшета — **P2** | ~2h

## App-level стан
- [ ] Глобальний стан теми (light/dark) — **P2** | ~1h
- [ ] Глобальний стан мови інтерфейсу (locale switcher, інтеграція з i18n-шаром) — **P1** | ~1h
- [ ] Глобальне управління активним polling job на рівні застосунку (щоб не дублювати запити при навігації між сторінками) — **P0** | ~2h

## Стани завантаження та помилок
- [ ] Уніфіковані loading-стани (skeleton/spinner) на рівні layout — **P1** | ~1h
- [ ] Глобальний error boundary та fallback UI для непередбачених помилок рендеру — **P1** | ~2h

### Примітки
- Це application shell проєкту: власний дашборд, а не переюзаний адмін-темплейт — навігація й layout мають бути мінімальними, але заточеними під flow «запуск → статус → результати → історія».
- Active-job polling варто підняти на рівень застосунку (а не окремої сторінки), щоб прогрес не губився при переході між вкладками дашборду.

### Залежності
- Управління активним polling job залежить від `frontend/TODO_state_data_layer.md` (реалізація polling-логіки та кешування).
- Перемикач мови залежить від `frontend/TODO_i18n_localization.md` (locale-файли, i18n-бібліотека).
- Маршрути статусу/результатів наповнюються компонентами з `frontend/TODO_pages_flows.md`.
